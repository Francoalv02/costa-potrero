//import pool from '../../../conexion/conexion.db.mjs';

//export async function buscarPorUsuario(usuario) {
//  const res = await pool.query('SELECT * FROM usuarios WHERE usuario = $1', [usuario]);
//  return res.rows[0];
//}
